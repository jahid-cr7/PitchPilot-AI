# PitchPilot AI — Frontend Production Build Environment
#
# This value is baked into the React bundle at Docker build time.
# It must be the external URL where the browser will reach the API.
# Example: https://api.pitchpilot.example.com

VITE_API_BASE_URL=http://localhost:8000
